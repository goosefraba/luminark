# Luminark v0.1.9

## Fixed

- Fixed Finder and `Open With` markdown launches so the viewer window is brought forward correctly and the launcher no longer remains the apparent result
- Folded pre-launch file-open events into startup routing while still handling post-launch file-open events through the live observer path

## Assets

- Apple Silicon build: `Luminark-0.1.9-macos-arm64.zip`
- Intel build: `Luminark-0.1.9-macos-x86_64.zip`

## Notes

- These builds are still unsigned for Developer ID distribution and are not notarized yet
- On first launch, macOS may still require `Open Anyway` or quarantine removal until notarized releases are in place
