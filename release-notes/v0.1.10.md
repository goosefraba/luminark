# Luminark v0.1.10

## Fixed

- Fixed a crash when opening markdown files in the packaged app caused by `Bundle.module` calling `fatalError` when the SPM resource bundle could not be found at the expected path

## Assets

- Apple Silicon build: `Luminark-0.1.10-macos-arm64.zip`
- Intel build: `Luminark-0.1.10-macos-x86_64.zip`

## Notes

- These builds are still unsigned for Developer ID distribution and are not notarized yet
- On first launch, macOS may still require `Open Anyway` or quarantine removal until notarized releases are in place
