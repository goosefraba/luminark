# Luminark v0.1.6

## Fixed

- Made the launcher drop-state window respect the active light/dark appearance instead of always rendering a light-toned panel

## Assets

- Apple Silicon build: `Luminark-0.1.6-macos-arm64.zip`
- Intel build: `Luminark-0.1.6-macos-x86_64.zip`

## Notes

- These builds are still unsigned for Developer ID distribution and are not notarized yet
- On first launch, macOS may still require `Open Anyway` or quarantine removal until notarized releases are in place
