# Luminark v0.1.7

## Fixed

- Fixed macOS Finder `Open With` and double-click launches so markdown files assigned to Luminark open directly instead of showing only the launcher drop zone on cold start

## Assets

- Apple Silicon build: `Luminark-0.1.7-macos-arm64.zip`
- Intel build: `Luminark-0.1.7-macos-x86_64.zip`

## Notes

- These builds are still unsigned for Developer ID distribution and are not notarized yet
- On first launch, macOS may still require `Open Anyway` or quarantine removal until notarized releases are in place
